🔑 Notes:

1) Replace YOUR_API_KEY with your OpenWeatherMap API key (sign up at https://openweathermap.org/api
).

2) zip=${pincode},IN → fetches Indian city by Pincode (you can change country code).

3) units=metric → gives Celsius instead of Kelvin.
